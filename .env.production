# Production Environment Variables
# Firebase configuration is already hardcoded in firebase.js
# No additional environment variables needed for production build

# React App Settings
GENERATE_SOURCEMAP=false
REACT_APP_VERSION=$npm_package_version